string = {}

--// 返回参数的大写表示
function string.upper(arg)

end

--// 返回参数的小写表示
function string.lower(arg)

end

--// 返回用replaceString替换findString后字符串
function string.gsub(mainString, findString, replaceString)

end

--// 返回mainString中findString的起始索引和结束索引找到字符串，如果未找到则返回nil
function string.find(mainString,findString, optionalStartIndex,optionalEndIndex)

end

--// 将传递的字符串arg反转后的字符串
function string.reverse(arg)

end

--// 返回格式化字符串
function string.format(...)

end

--// 返回输入参数的内部数字表示
function string.char(arg)

end

--// 返回输入参数的内部字符表示
function string.byte(arg)

end

--// 返回传递字符串arg的长度。
function string.len(arg)

end

--// 返通过重复相同的字符串n次返回一个字符串
function string.rep(arg, n)

end